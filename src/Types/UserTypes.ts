export type SignUpData = {
  firstName: string;
  lastName: string;
  userName: string;
  email: string;
  password: string;
};
export type User = {
  firstName: string;
  lastName: string;
  userName: string;
  email: string;
  token: string;
};
